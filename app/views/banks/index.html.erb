<div class="container">
  <div class="row">
    <div class="col text-center">
      <%= link_to 'Create new bank', new_bank_path, class:"btn btn-primary" %>
    </div>
  </div>
</div>

<div id="page-content">
  <h1 class="text-center mt-4">Index of banks</h1>
   <div class="container">
  <% @banks.each do |bank| %>
    <div class="row justify-content-md-center">
      <div class="col-8 mt-4">
        <div class="card text-center shadow mb-5 bg-white rounded">
          <div class="card-header font-italic">
            by Rubén Marín
          </div>
          <div class="card-body">
            <h5 class="card-title"><%= link_to bank.name, bank_path(bank), class: "text-success" %></h5>
            <p class="card-text"><%= truncate(bank.code, length: 20) %></p>
            <%= link_to "Show", bank_path(bank), class: "btn btn-outline-success" %>
            <%= link_to "Edit", edit_bank_path(bank), class: "btn btn-outline-info" %>
            <%= link_to "Delete", bank_path(bank), class: "btn btn-outline-danger", method: :delete, data: {confirm: "Are you sure?"} %>
          </div>
          <div class="card-footer text-muted">
            <small>Created <%= time_ago_in_words(bank.created_at) %> ago, 
            edited <%= time_ago_in_words(bank.updated_at) %> ago</small>
          </div>
        </div>
      </div>
    </div>
  <% end %>
</div>

<div class="container">
  <div class="row">
    <div class="col text-center">
      <%= link_to 'Create new bank', new_bank_path, class:"btn btn-primary" %>
    </div>
  </div>
</div>